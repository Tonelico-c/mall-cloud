<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.situ.mall.mapper.OrderMapper">

    <resultMap type="com.situ.mall.pojo.vo.OrderVO" id="orderMap">
        <id property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="payment" column="payment"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <collection property="orderItemList" ofType="com.situ.mall.pojo.entity.OrderItem">
            <result property="productId" column="product_id"/>
            <result property="productName" column="product_name"/>
            <result property="currentUnitPrice" column="current_unit_price"/>
            <result property="quantity" column="quantity"/>
            <result property="productImage" column="product_image"/>
        </collection>
    </resultMap>

    <select id="listItem" resultMap="orderMap">
        SELECT *
        FROM orders AS o INNER JOIN order_item AS oi
        ON o.order_no = oi.order_no
        WHERE o.user_id = #{userId}
        ORDER BY o.create_time DESC

    </select>
</mapper>
